import React from 'react';
import { Typography } from "@material-ui/core";

export function Delivered() {
    return (
        <Typography variant="h5" component="h2">Your CO2 emissions have been offset with Climeworks.</Typography>
    );
}